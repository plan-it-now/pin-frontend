import React, { Component } from 'react';
import {
  Text,
  View
} from 'react-native';

import { Icon, Container, Col, Grid } from 'native-base';

const inputQuery = () => {
  return(
    <Container style={{flex:1}}>
      <View style={{marginTop:400}}>
        <Text style={{fontSize: 50}}>COBA</Text>
      </View>
    </Container>
  )
}

export default inputQuery
